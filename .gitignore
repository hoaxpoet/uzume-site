.DS_Store
node_modules/
dist/
**/.build/
**/__pycache__/
*.py[cod]
.impeccable/config.local.json

# astro
.astro/

.env
.env.*

# Media lives in R2 at media.uzume.io, never in git (CLAUDE.md). A 1.4 GB capture
# once landed untracked in docs/planning/ one `git add .` away from a commit.
*.mov
*.mp4
*.m4v

# wrangler dev's local state, from the preview config in .claude/launch.json
.wrangler/
