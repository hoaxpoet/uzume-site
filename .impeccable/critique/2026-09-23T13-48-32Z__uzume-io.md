---
target: uzume.io and uzume.io/gallery/
total_score: 27
max_score: 36
na_heuristics: 7
p0_count: 2
p1_count: 3
target_identity: "url:https://uzume.io/"
timestamp: 2026-09-23T13-48-32Z
slug: uzume-io
---
Method: dual-agent (A: design review, isolated · B: detector + browser evidence, isolated).

## Design Health Score — 27/36 (heuristic 7 n/a)

| # | Heuristic | Score | Key Issue |
|---|---|---|---|
| 1 | Visibility of System Status | 2 | Pause button reads "Pause" over a frozen poster; render() keys off the stopped flag, not playback. Native spinners centred on every gallery frame. |
| 2 | Match System / Real World | 4 | Nothing wrong. |
| 3 | User Control and Freedom | 3 | Pause is homepage-only, >=768px only. /gallery has no site-level stop control. |
| 4 | Consistency and Standards | 2 | One NotifyForm, two consent promises. Six cards, one destination. Nav hrefs 307. |
| 5 | Error Prevention | 3 | Form solid; nothing prevents the "where's the download" dead end. |
| 6 | Recognition Rather Than Recall | 3 | 8,322px gallery, no index or count; anchors exist but unexposed. |
| 7 | Flexibility and Efficiency | n/a | Linear reads, no repeat task. |
| 8 | Aesthetic and Minimalist Design | 3 | Native video chrome + spinner on the art, on the surface whose job is the art. |
| 9 | Error Recovery | 4 | Source-verified; neither agent submitted to the live endpoint. |
| 10 | Help and Documentation | 3 | Docs in nav, CONTRIBUTING linked twice. |

Trend: 22/40 -> 23/40 -> 27/36. Not like-for-like (nine heuristics vs ten). As percentages: 55% -> 57.5% -> 75%.

## Design Specificity Verdict

Homepage authored above the fold, category-default below. The wordmark handoff (one view-timeline on the hero h1, two properties, base styles as the finished state), the glyph-shaped scrim, and --uz-loop-position are specific to this product. Below the fold: centred email band, 2-up poster grid, alternating rows — swap the name and footage and it serves any Mac utility.

Gallery is category-default rescued by writing. Vertical stack of 16:9 video controls with captions is the default demo-reel shape; no First Opening motif, no authored relationship between scenes. The caption voice carries the entire surface.

Deterministic: the .astro skip re-verified with a fresh control — byte-identical 616-byte file returns exit 2 / 13 findings as .html, exit 0 / [] as .astro. Live URL scans all exit 2. Home desktop 6 (tight-leading, line-length x3, em-dash-overuse, low-contrast 2.9:1); home 375 3 (low-contrast 3.0:1); gallery 9 both widths (heading-rhythm x8). Project gates all exit 0.

Overlays: none. Correction to last run — CSP did NOT block injection; 'unsafe-inline' is present and permits it. The overlay flow was skipped by instruction, not obstacle.

## What's Working

1. The copy carries the differentiation in one clause: "so what you see follows the music instead of twitching at its volume", paired with the streaming warmup stated as part of the explanation rather than hidden.
2. Zero layout shift, measured two ways: CLS 0.00000, zero buffered layout-shift entries, and all 66 tracked boxes identical between first paint and +7s. TTFB 44ms, DCL 122ms.
3. The form's success state is designed as an outcome, including "Used the wrong address?" for Kit's double opt-in.

## Priority Issues

- [P0] Hero claim fails WCAG contrast over the video. No scrim element exists; the claim relies on three text-shadows. DOM computation sees the body colour and passes. Pixel sampling: 2.9:1 desktop (26px/600, 3:1 threshold, marginal fail); at 375px the claim drops to 18px, below the 18.66px bold cutoff, so the threshold becomes 4.5:1 — measured 3.0:1, a clear failure. The wordmark is exempt as a brand name; the claim is not. -> /impeccable audit
- [P0] The page never states release status. No "no build", "nothing to download", "source is public", "buildable" or "notarized" anywhere on /. CLAUDE.md's copy law requires the distinction. Caused by the "empty the hero" commit, which cut the status line on the argument that the form label carried it. -> /impeccable clarify
- [P1] Primary action below the fold; platform gate above it. At 1366x700: hero stage 544px, specs top 618, input top 737. Inverts DESIGN.md:202 — nothing gates giving an email address. -> /impeccable layout
- [P1] Gallery spends 1.52 MB on posters before anything plays. video poster has no lazy-loading; all eight JPEGs fetch on load = 1,553,503 B, 95% of page weight, identical at 375 and 1440. AVIF equivalents total 978,994 B: 574 KB unreachable because poster cannot content-negotiate. skein.jpg 571 KB and ferrofluid-ocean.avif 373 KB against a ~15 KB median. murmuration 14.55/14.70 MB, over the repo's own 12 MB ceiling. Safari pays 53.26 MB vs Chrome 39.65 MB. -> /impeccable optimize
- [P1] Gallery's only controls have no visible focus indicator. Eight videos fall back to Chrome's UA ring, clipped by .uz-media-frame__content overflow:hidden. Observed by A only; B did not corroborate — verify before fixing. -> /impeccable audit

## Persona Red Flags

- Listener from a shared /gallery link: no stop control at any width; never learns the platform or the release status; the flash-safety sentence is fused into a paragraph about audio tracks (both .intro__note paragraphs compute margin 0); the only closing heading is a contributor pitch.
- Contributor: best served — bylines, honest "(ported)" credit, CONTRIBUTING linked twice, Milkdrop lineage. Gaps: nothing says buildable today; nothing indicates repertoire scale.
- Keyboard/screen-reader: strong overall. Fails: invisible focus rings on eight gallery videos; six links, one destination; Pause announcing "Pause" when nothing plays.
- Photosensitive visitor: worst served. A loop begins within 400ms on desktop without prefers-reduced-motion, and the homepage carries zero flash-safety language.

## Minor Observations

- [P2] Six scene cards, one destination. ids exist; the lychee workaround solved tooling at the design's expense.
- [P2] One form, two consent promises. The success panel shows the gallery's version, so a homepage signup learns the real terms after committing.
- [P2] Hero footage unlabelled — no "Murmuration", "engine" or "recorded" on /. Direct tension: the credit line was removed by instruction, and BRAND.md conditions the looping exemption on footage being labelled as engine output.
- heading-rhythm x8 persists: 20px above, 44px below. The #48 fix addressed size (16->28px), not spacing.
- uzume-icon.png is 69.5 KB rendered at 64x64 — ~4x oversized in area at DPR 2.
- Nav links 307-redirect: href="/gallery" and "/docs" both redirect to the slashed form.
- Six preset-card imgs declare no width/height (no shift; aspect-ratio holds them).
- H1/H2 run 0.92 line-height, tighter than the flagged 1.08.
- .signup__what and .notify centre independently; icon's left edge ~40px inboard of the input's.
- --content-default 72rem on /gallery vs a local 64rem column on /.
- No theme-color; /favicon.ico 404s (harmless). OG image 200 at exactly 1280x640.

## Questions

1. If the murmuration is the best argument, why is it the one clip left unnamed?
2. What would the page look like if "there is no build yet" were the headline rather than the omission?
3. Is the gallery an experience or an archive? The product's claim is that it sequences scenes into an arc; the page argues the opposite.
4. The Pause button is the only place the chrome admits the footage is footage. What else could it admit?
