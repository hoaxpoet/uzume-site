# Kit (ConvertKit) form id for the "notify me" capture on the landing page.
# Set it in the Cloudflare project too — without it the hero falls back to a
# link to GitHub Releases rather than shipping a form that posts nowhere.
KIT_FORM_ID=
