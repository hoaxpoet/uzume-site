---
// Replaces Starlight's light/dark picker with nothing. The rest of the site is
// hard-committed to dark in Base.astro — "darkness is the condition" — so /docs
// following the visitor's OS preference was the one place the brand fell away.
// Pinned dark by the inline script in astro.config.mjs; this removes the
// control that would contradict it.
---
